<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cucook.moc.admin.dao.AdminNoticeDAO">

    <!-- 공지사항 목록: cursor 기반(lastNoticeId + limit) -->
    <select id="selectNoticeList"
            parameterType="com.cucook.moc.admin.dto.request.AdminNoticeSearchRequestDTO"
            resultType="com.cucook.moc.admin.vo.AdminNoticeVO">
        SELECT
        notice_id    AS noticeId,
        title        AS title,
        content      AS content,
        image_url    AS imageUrl,
        is_pinned    AS isPinned,
        is_visible   AS isVisible,
        created_id   AS createdId,
        created_date AS createdDate,
        updated_id   AS updatedId,
        updated_date AS updatedDate
        FROM tb_notice
        WHERE is_visible = 'Y'
        <if test="keyword != null and keyword != ''">
            AND title LIKE '%' || #{keyword} || '%'
        </if>
        <if test="lastNoticeId != null">
            AND notice_id &lt; #{lastNoticeId}
        </if>
        ORDER BY notice_id DESC
        <if test="limit != null">
            FETCH FIRST #{limit} ROWS ONLY
        </if>
    </select>

    <!-- 단일 공지 조회 -->
    <select id="selectNoticeById"
            parameterType="long"
            resultType="com.cucook.moc.admin.vo.AdminNoticeVO">
        SELECT
        notice_id    AS noticeId,
        title        AS title,
        content      AS content,
        image_url    AS imageUrl,
        is_pinned    AS isPinned,
        is_visible   AS isVisible,
        created_id   AS createdId,
        created_date AS createdDate,
        updated_id   AS updatedId,
        updated_date AS updatedDate
        FROM tb_notice
        WHERE notice_id = #{noticeId}
    </select>

    <!-- 생성 -->
    <insert id="insertNotice" parameterType="com.cucook.moc.admin.vo.AdminNoticeVO">
        <selectKey keyProperty="noticeId" resultType="long" order="BEFORE">
            SELECT seq_tb_notice.NEXTVAL FROM dual
        </selectKey>
        INSERT INTO tb_notice (
        notice_id, title, content, image_url,
        is_pinned, is_visible,
        created_id, created_date
        ) VALUES (
        #{noticeId},
        #{title},
        #{content},
        #{imageUrl},
        #{isPinned},
        #{isVisible},
        #{createdId},
        SYSTIMESTAMP
        )
    </insert>

    <!-- 수정 -->
    <update id="updateNotice" parameterType="com.cucook.moc.admin.vo.AdminNoticeVO">
        UPDATE tb_notice
        SET title        = #{title},
        content      = #{content},
        image_url    = #{imageUrl},
        is_pinned    = #{isPinned},
        updated_id   = #{updatedId},
        updated_date = SYSTIMESTAMP
        WHERE notice_id    = #{noticeId}
    </update>

    <!-- 고정/해제 -->
    <update id="updateNoticePin">
        UPDATE tb_notice
        SET is_pinned    = #{isPinned},
        updated_id   = #{adminUserId},
        updated_date = SYSTIMESTAMP
        WHERE notice_id    = #{noticeId}
    </update>

    <!-- 삭제 (실제 삭제) -->
    <delete id="deleteNotice" parameterType="long">
        DELETE FROM tb_notice
        WHERE notice_id = #{noticeId}
    </delete>

</mapper>
